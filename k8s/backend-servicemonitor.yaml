apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-monitor
  namespace: default
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: quiz-backend
  namespaceSelector:
    any: true
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
